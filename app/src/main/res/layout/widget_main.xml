<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="5dip"
    android:paddingRight="5dip"
    android:background="@drawable/roundedbutton">

    <ImageView
        android:id="@+id/ivWidgetIcon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_01"
        android:scaleX="1.2"
        android:scaleY="1.2"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/tvWidgetTemperature"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/ivWidgetIcon"
        android:text="@string/temperature"
        android:textColor="@color/weather_white"
        android:gravity="center_horizontal"
        android:singleLine="true"
        android:textSize="20dp"
        android:layout_marginTop="-7dp"
         />
    <TextView
        android:id="@+id/tvWidgetCityName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvWidgetTemperature"
        android:textColor="@color/weather_white"
        android:layout_alignParentBottom="true"
        android:textSize="12dp"
        android:gravity="center_horizontal"
        android:singleLine="true"
        android:layout_marginTop="-3dp"
        />

</RelativeLayout>